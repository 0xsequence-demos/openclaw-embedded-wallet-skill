# Copy to .env (or export env vars in your shell)
# Get these from https://sequence.build (create a project + enable Embedded Wallet).
SEQUENCE_WAAS_PROJECT_ACCESS_KEY=
SEQUENCE_WAAS_CONFIG_KEY=

# Optional: base URL for your deployed wallet-link UI
SEQUENCE_WAAS_WORKER_URL=https://moltbot-wallet.taylanpince.workers.dev

# Required for the `balances` command.
# For Polygon:
#   https://polygon-indexer.sequence.app
SEQUENCE_INDEXER_ACCESS_KEY=

# Optional override per-chain (otherwise defaults to Sequence public indexers)
# SEQUENCE_INDEXER_URL=
