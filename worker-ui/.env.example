# Copy to .env and fill values.
# Get these from https://sequence.build (create a project + enable Embedded Wallet).
VITE_PROJECT_ACCESS_KEY=
VITE_WAAS_CONFIG_KEY=

# Optional: used to show balances in the UI via Sequence indexer.
# If you omit it, the UI will still work for linking; balances will be hidden.
VITE_POLYGON_INDEXER_ACCESS_KEY=
